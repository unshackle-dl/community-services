A community driven collection of DRM-Free services

## Usage:
Clone repository:

`git clone https://github.com/unshackle-dl/community-services`

Add folder to `unshackle.yaml`:

```
directories:
    services: "path/to/services"
```

See help text for each service:

`unshackle dl SERVICE --help`
